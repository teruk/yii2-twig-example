<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="{{ app.homeUrl }}">Basic</a>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="active"><a href="{{ url.to(['site/index']) }}">{{ t('app/navigation', 'home', null, cookieGet('language', 'en-US')) }}</a></li>
        <li><a href="{{ url.to(['site/about']) }}">{{ t('app/navigation', 'about', null, cookieGet('language', 'en-US')) }}</a></li>
        <li><a href="{{ url.to(['site/contact']) }}">{{ t('app/navigation', 'contact', null, cookieGet('language', 'en-US')) }}</a></li>
        <!-- if case -->
        {% if cookieGet('language', 'en-US') == 'de-DE' %}
          <li><a href="{{ url.to(['site/impressum']) }}">{{ t('app/navigation', 'impressum', null, cookieGet('language', 'de-DE')) }}</a></li>
        {% endif %}
        <li>{{ cookieGet('language') }}</li>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>